# ──────────────────────────────────────────────────────────────────────────────
# requirements.prod.txt  —  Production / Railway deployment
# ──────────────────────────────────────────────────────────────────────────────
# Lean build for cloud deployment. Heavy ML packages excluded (torch etc.)
# App runs fully on physics-based fallbacks when ML packages are absent.
# For local development with full AI: pip install -r requirements.dev.txt
# ──────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0

# Auth
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# Data / config
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# HTTP + image
httpx>=0.27.0
pillow>=10.0.0

# Numerics / ML (lightweight only)
numpy>=1.26.0
scikit-learn>=1.4.0
xgboost>=2.0.0
