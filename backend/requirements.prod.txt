# ──────────────────────────────────────────────────────────────────────────────
# requirements.prod.txt  —  Production / Railway deployment
# ──────────────────────────────────────────────────────────────────────────────
# This is the LEAN build used for cloud deployment (Railway, Render, Fly.io).
# Heavy ML packages (torch, torchvision, ultralytics) are intentionally excluded
# because they exceed Railway's free-tier build limits (~2.4 GB for torch alone).
#
# The app runs FULLY without them — all AI layers have synthetic fallbacks:
#   • Tree detection    → _synthetic_trees()   in analysis_pipeline.py
#   • Segmentation      → _synthetic_seg()     in analysis_pipeline.py
#   • Flood model       → physics formula      in real_env_data.py
#   • Buildability      → compute_buildability() in real_env_data.py
#
# For local development with full AI features, use requirements.txt instead:
#   pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────────────────────

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
httpx>=0.27.0
pillow>=10.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
xgboost>=2.0.0
