[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = [
  "pip install --break-system-packages -r requirements.prod.txt"
]

[start]
cmd = "/mise/installs/python/3.13.12/bin/python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
